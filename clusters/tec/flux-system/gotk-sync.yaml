# This manifest was generated by flux. DO NOT EDIT.
apiVersion: source.toolkit.fluxcd.io/v1beta2
kind: GitRepository
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 1m0s
  ref:
    branch: main
  secretRef:
    name: flux-system
  url: ssh://git@github.com/pbar1/pbcloud
  ignore: |
    **/Taskfile.yaml
---
apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: flux-system
  namespace: flux-system
spec:
  interval: 10m0s
  path: ./clusters/tec
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  # FIXME: Depends on secret/sops-gpg already existing
  decryption:
    provider: sops
    secretRef:
      name: sops-gpg
  postBuild:
    # FIXME: Depends on configmap/cluster-settings and secret/cluster-secrets
    # already existing
    substituteFrom:
    - kind: ConfigMap
      name: cluster-settings
    - kind: Secret
      name: cluster-secrets
