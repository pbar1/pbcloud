apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: prowlarr
  namespace: media
spec:
  interval: 24h
  chart:
    spec:
      chart: prowlarr
      sourceRef:
        kind: HelmRepository
        name: geek-cookbook
        namespace: flux-system
  values:
    image:
      repository: ghcr.io/linuxserver/prowlarr
      tag: latest
      pullPolicy: Always

    env:
      TZ: America/Los_Angeles
      PUID: 1000
      PGID: 100

    podAnnotations:
      # FIXME: failed to get container spec opts: failed to generate apparmor spec opts: apparmor is not supported
      container.apparmor.security.beta.kubernetes.io/prowlarr: unconfined

    podSecurityContext:
      fsGroup: 100
      fsGroupChangePolicy: OnRootMismatch
      capabilities:
        drop:
        - all
      seccompProfile:
        type: RuntimeDefault

    persistence:
      config:
        enabled: true
        type: hostPath
        hostPath: /data/general/config/prowlarr
        mountPath: /config

    ingress:
      main:
        enabled: true
        annotations:
          cert-manager.io/cluster-issuer: letsencrypt-production
          traefik.ingress.kubernetes.io/router.entrypoints: websecure
        hosts:
        - host: "prowlarr.${DOMAIN}"
          paths:
          - path: /
            pathType: Prefix
        tls:
        - hosts:
          - "prowlarr.${DOMAIN}"
          secretName: prowlarr-tls
