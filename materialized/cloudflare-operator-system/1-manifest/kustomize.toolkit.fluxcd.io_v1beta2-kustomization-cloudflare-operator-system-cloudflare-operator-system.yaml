apiVersion: kustomize.toolkit.fluxcd.io/v1beta2
kind: Kustomization
metadata:
  name: cloudflare-operator-system
  namespace: cloudflare-operator-system
spec:
  force: false
  interval: 24h
  path: ./materialized/cloudflare-operator-system
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
