apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: atuin
  namespace: atuin
spec:
  force: false
  interval: 24h
  path: ./materialized/atuin
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
