apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: 1password
  namespace: 1password
spec:
  force: false
  interval: 24h
  path: ./materialized/1password
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
    namespace: flux-system
